import Link from "next/link";
export default async function ThankYou({ searchParams }: { searchParams: Promise<{ paket?: string }> }) { const params = await searchParams; const paket = params.paket || "GROWTH"; return <main className="noise" style={{ minHeight: "100vh", padding: "10vh 8vw" }}><p className="sans" style={{ letterSpacing: 2, fontSize: 11 }}>FUNNELPAGE.ID / KONFIRMASI</p><h1 style={{ fontSize: "clamp(4rem,10vw,10rem)", lineHeight: .82, margin: "40px 0" }}>Pesanan<br /><i>diterima.</i></h1><p className="sans" style={{ maxWidth: 450, lineHeight: 1.7 }}>Paket <strong>{paket}</strong> sudah kami catat. Tim kami akan menghubungi kamu melalui WhatsApp untuk langkah berikutnya.</p><Link className="sans" href="/" style={{ display: "inline-block", marginTop: 30, padding: "18px 24px", background: "var(--ink)", color: "var(--acid)" }}>KEMBALI KE BERANDA ↗</Link></main>; }
